spring:
  application:
    name: product-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-db

server:
  port: 8990
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.thehappycode: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080
