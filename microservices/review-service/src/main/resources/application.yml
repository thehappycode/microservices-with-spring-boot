spring:
  application:
    name: review-service
server:
  port: 8992
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.thehappycode: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080
