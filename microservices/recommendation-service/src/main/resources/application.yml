spring:
  application:
    name: recommendation-service
server:
  port: 8991
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.thehappycode: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080
